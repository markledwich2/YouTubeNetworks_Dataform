config {
  type: "table"
}

with d as (
  select distinct domain from links
)
select domain, decode(domain, 'bitchute.com', 'BitChute', 'rumble.com', 'Rumble', 'youtube.com', 'YouTube', 'youtu.be', 'YouTube') platform
from d
where platform is not null
