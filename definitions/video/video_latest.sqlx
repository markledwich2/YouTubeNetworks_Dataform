config { type: "table" }

-- video_latest
with v as (
    select *
         , row_number() over (
        partition by video_id
        order by
            updated desc
        ) as age
    from ${ref("video")})
select *
from v
where age = 1;